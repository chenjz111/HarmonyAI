# Document page artwork

Source: Owner-supplied `第二个页面.zip`, 2026-09-12. Original archive unchanged.

- `background.png`: copied unchanged from `第二个页面/背景.png`, 853 × 1844.
- `camera.png`: copied unchanged from `第二个页面/照相机.png`, 1952 × 2272, RGB (not transparent). The UI uses a circular CSS viewport and centered enlargement to exclude the baked checkerboard outside the mint disk.
- `leaf.svg`, `document.svg`: small code-native UI icons, not extracted source illustration layers.
- The two standalone musical-note PNGs in the ZIP were not imported because their checkerboard is baked into RGB, and the background already includes musical notes.

No image generation or paid provider requests were used for this page pair.
