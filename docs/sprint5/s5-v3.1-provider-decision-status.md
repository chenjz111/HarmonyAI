# HarmonyAI Sprint 5 V3.1 Provider Decision Status

> Status: **PROVISIONAL**
> This document records repository facts and open decisions. It contains no credentials and does not approve a Provider.

| Capability | Current Interface | Current Implementation | Current config (names only) | Known candidate | Decision status | Blocking question |
| --- | --- | --- | --- | --- | --- | --- |
| OCR | Document upload/OCR service | PaddleOCR adapter exists and has explicit failure handling; V3.1 1～3 page/relevance path still needs real validation | Existing OCR runtime settings | PaddleOCR | NEEDS_REAL_VALIDATION | Can ordered multi-page OCR meet accuracy, latency and failure-routing acceptance? |
| LLM Understanding | V3 understanding provider adapter | Qwen cloud/local adapter foundation exists; authoritative integration does not yet prove the complete V3.1 real path | `QWEN_BASE_URL`, `QWEN_API_KEY`, `QWEN_MODEL`, `LOCAL_QWEN_BASE_URL`, `LOCAL_QWEN_API_KEY`, `LOCAL_QWEN_MODEL` | Qwen cloud/local | NEEDS_REAL_VALIDATION | Which approved deployment and structured-output policy becomes production authority? |
| LLM Diagnosis | Agent 2 diagnosis interface | Schemas/fallback concepts exist; production Query Builder → RAG → Qwen → validators path is not implemented on the authoritative integration baseline | Provider variables not frozen | Qwen candidate | NOT_IMPLEMENTED | Which model, syndrome whitelist and retry/abstain policy are approved? |
| Embedding | Knowledge retrieval embedding boundary | Legacy/demo retrieval uses `hash-v1`; it is not acceptable as production semantic retrieval | No approved V3 production embedding config | Not frozen | NEEDS_DECISION | Which embedding model, dimension, normalization and version policy are approved? |
| Vector DB | RAG store/retrieval boundary | Chroma code/dependency exists; approved V3 corpus ingestion and real-mode retrieval are not yet validated | Existing Chroma storage settings | Chroma | NEEDS_REAL_VALIDATION | What corpus/version/index lifecycle and retrieval acceptance evidence are required? |
| Music Generation | `MusicGenerationProvider` and task adapter | Provider protocol/task states exist; current adapter resolves to not-configured rather than sending a production generation request | Music Provider config not frozen | Qwen-Music/Fun-Music; MiniMax backup; Stable Audio local candidate | NEEDS_DECISION | Which Provider is approved for licensing, API capability, latency, cost and playable-asset delivery? |
| ASR | Voice/Narrative provider decision record | No approved production ASR path is required by the V3.1 primary flow because Narrative is removed | ASR config not frozen | Existing comparison candidates | NEEDS_DECISION | Defer for V3.1 core, or retain a compatibility Provider commitment? |

Decision status vocabulary:

- `KEEP`: current implementation is approved without material V3.1 change.
- `NEEDS_DECISION`: Owner/teacher/provider choice is unresolved.
- `NOT_IMPLEMENTED`: required production call path is absent from the authoritative baseline.
- `NEEDS_REAL_VALIDATION`: an interface or implementation exists, but V3.1 real-mode evidence is incomplete.
